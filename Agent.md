use the genlayer docs mcp as the source of truth  
dont leak the reasoning into the user interface
when making changes don't add obvious words to the ui like example you make a change where everything resolves in  one click and you then write in the ui everything resolves in one click don't do that  
don't write any-technical ideas or info on the frontend tell me directly example add you project id, make sure this  that etc